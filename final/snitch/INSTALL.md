# Blockchain directory init

```sh
mkdir /blockchain
cd /blockchain
forge init --no-git
# forge install @openzeppelin=OpenZeppelin/openzeppelin-contracts --no-git
forge remappings > remappings.txt
```
